#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../mocha-only-detector/bin/mocha-only-detector" "$@"
  ret=$?
else 
  node  "$basedir/../mocha-only-detector/bin/mocha-only-detector" "$@"
  ret=$?
fi
exit $ret
